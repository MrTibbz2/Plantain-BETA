can you please refactor the ui? first remove all themes not dark or light, and refactor the look to be cleaner and more obvious without looking cartoony. ensure still that its navigable accessibly, the fonts are readable, and it meets all the accessibility standards for people using certain tools. then, also refactor the API. there should now be clear seperation between select config: which returns fail or not, and sets the current mapping to the filename passed in. however, GetConfig (or load or whatever, name it applicably) should be the one passing the current values of a certain config to the ui. similarly, the UI should reflect this, having an obvious difference between editing and simply selecting a config. also, make the ui scalable and fit on most screens without scrolling required, yet fitting all the elements. maybe for this have a button to open an editor to edit configs. also, add me a few more themes, but not just colours. make them change the overall look and feel of the UI and also add animations. focus on making the themes production ready, not a playground. make them clean, with loading anims and make sure they look how a professional project should. however, have a little fun here and there, but only in tidbits.